<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TaskManagerApp.Pages.UserTaskManagerPage"
             xmlns:viewmodels="clr-namespace:TaskManagerApp.ViewModels"
             Title="User TaskManager"
             x:Name="UserTaskPage">

    <Grid ColumnDefinitions="200,*" RowDefinitions="*">

        
        <CollectionView Grid.Column="0"
                        ItemsSource="{Binding Users}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedUser}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5" BackgroundColor="LightGray" CornerRadius="10">
                        <Label Text="{Binding UserName}" FontSize="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <ScrollView Grid.Column="1" Padding="10">
            <VerticalStackLayout>
                <Label Text="Tasks Calendar"
                       FontSize="20"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       Margin="0,0,0,10"/>

                <CollectionView ItemsSource="{Binding SelectedUser.Tasks}">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="1"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Margin="5" Padding="10" BorderColor="DarkGray" CornerRadius="8">
                                <VerticalStackLayout>
                                    <Label Text="{Binding DueDate, StringFormat='{0:dd/MM/yyyy}'}" 
                                           FontSize="14" FontAttributes="Bold" />
                                    <Label Text="{Binding Title}" FontSize="16"/>

                                    <Button Text="Delete"
                                            BackgroundColor="Red"
                                            TextColor="White"
                                            Command="{Binding Source={x:Reference UserTaskPage}, Path=BindingContext.DeleteTaskCommand}"
                                            CommandParameter="{Binding .}" />   
                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>